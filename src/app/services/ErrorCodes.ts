export enum ErrorCodes {
  ERR_NOT_LOGGED_IN = 'Not logged into Realm',
  ERR_OFFLINE = 'Device is offline',
  ERR_CALENDAR_EXISTS = 'A calendar already exists for that year',
  ERR_SUBJECT_EXISTS = 'A subject already exists with that code',
  ERR_TERM_EXISTS = 'A term already exists that starts and ends at the same time',
  ERR_TEACHER_EXISTS = 'A teacher already exists with that email',
  ERR_COURSE_EXISTS = 'A course already exists with that code',
  ERR_CLASS_EXISTS = 'A class already exists with that code'
}
